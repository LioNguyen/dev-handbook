/**
 * Zustand Demo Page
 * Zustand doesn't require a provider, but we create a page component for consistency
 */

import { AuthDemo } from './AuthDemo'

export function ZustandDemoPage() {
  return <AuthDemo />
}
